# Development Environment Variables (Encrypted with SOPS)
# This file contains sensitive configuration for development
# Decrypt using: sops -d env.dev.enc.yaml

# Server Configuration
PORT: 3000
NODE_ENV: development

# CORS Configuration
ALLOWED_ORIGINS: http://localhost:3000,http://localhost:3001

# Database Configuration
DB_HOST: localhost
DB_PORT: 27017
DB_NAME: api_dev
DB_USER: api_dev_user
DB_PASSWORD: dev_password_123

# JWT Configuration
JWT_SECRET: dev-jwt-secret-key-change-in-production
JWT_EXPIRES_IN: 24h

# Logging
LOG_LEVEL: debug

# Rate Limiting
RATE_LIMIT_WINDOW_MS: 900000
RATE_LIMIT_MAX_REQUESTS: 100

# MongoDB Connection (for local development)
MONGO_URI: mongodb://api_dev_user:dev_password_123@localhost:27017/api_dev?authSource=admin
